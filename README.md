# PrettyPet — Frontend

**PrettyPet** — это современное веб-приложение для поиска, создания и управления исполнителями для пэт-проектов.

Данный репозиторий содержит **фронтенд-часть** проекта.

## О проекте

PrettyPet помогает пользователям:

- Создавать свои пэт-проекты
- Находить исполнителей и единомышленников
- Управлять командами и задачами

## Технологический стек

- **React** + **TypeScript**
- **Vite** (сборка и dev-server)
- **Redux Toolkit** (глобальное состояние)
- **Ant Design** (UI-компоненты)
- **Tailwind CSS** (утилитарные стили)
- Архитектура: **FSD (Feature-Sliced Design)**

## Требования

- **Node.js** >= 18.x
- **npm** >= 9.x

## Установка и запуск

1. **Клонируйте репозиторий:**

   ```bash
   git clone https://github.com/your-username/pretty-pet-frontend.git
   cd pretty-pet-frontend/frontend-react
   ```

2. **Установите зависимости:**

   ```bash
   npm install
   ```

3. **Запустите проект в режиме разработки:**

   ```bash
   npm start
   ```

   После запуска приложение будет доступно по адресу, который покажет Vite (обычно http://localhost:5173).

4. **Сборка для production:**

   ```bash
   npm run build
   ```

5. **Предпросмотр production-сборки:**

   ```bash
   npm run preview
   ```

## Инструкция по коммитам

Для единообразного оформления коммитов используется **Commitizen**.  
При этом **Husky** автоматически запускает проверки с помощью **Lint Staged** перед фиксацией изменений.

1. **Добавьте изменённые файлы**:

   ```bash
   git add <имя_файла>
   ```

   или все сразу:

   ```bash
   git add .
   ```

2. **Запустите процесс коммита**:

   ```bash
   git commit
   ```

3. Дождитесь завершения проверки.
   Если найдены ошибки — исправьте их и повторите попытку.

4. После успешной проверки появится интерфейс **Commitizen**.  
   Следуйте подсказкам и завершите написание коммита.

## Проверка и форматирование кода

Также можно выполнить проверку кода вручную.

```bash
npm run tsc        # Проверка типов TypeScript
npm run lint       # Проверка кода ESLint
npm run lint:fix   # Проверка и исправление кода ESLint
npm run format     # Форматирование Prettier
npm run check      # Комплексная проверка и исправление
```

## Структура проекта

Проект построен по принципам FSD (Feature-Sliced Design). Подробнее см. [src/README_FSD.md](src/README_FSD.md).

## Контакты

Если у вас есть идеи, предложения или найденные ошибки — создайте **issue** или отправьте **pull request**.  
Мы открыты к сотрудничеству и рады любым улучшениям проекта!
