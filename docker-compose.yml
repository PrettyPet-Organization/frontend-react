version: '3.8'

services:
  frontend:
    build: .
    container_name: prettypet-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./logs:/var/log/nginx
    environment:
      - NODE_ENV=production